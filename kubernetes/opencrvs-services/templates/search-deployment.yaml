apiVersion: v1
kind: Service
metadata:
  labels:
    app: search
  name: search
spec:
  ports:
    - port: 9090
      targetPort: 9090
  selector:
    app: search
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: search
  name: search
spec:
  replicas: 1
  selector:
    matchLabels:
      app: search
  template:
    metadata:
      labels:
        app: search
    spec:
      containers:
        - name: search
          image: "opencrvs/ocrvs-search:{{ .Values.image.tag }}"
          env:
            - name: APPLICATION_CONFIG_URL
              value: http://config:2021/
            - name: CERT_PUBLIC_KEY_PATH
              value: /secrets/public-key.pem
            - name: ES_HOST
              value: elasticsearch:9200
            - name: FHIR_URL
              value: http://hearth:3447/fhir
            - name: HEARTH_MONGO_URL
              value: mongodb://mongo1/hearth-dev
            - name: NODE_ENV
              value: development
            - name: USER_MANAGEMENT_URL
              value: http://user-mgnt:3030/
          ports:
            - containerPort: 9090
              protocol: TCP
          volumeMounts:
            - mountPath: /secrets/public-key.pem
              name: public-key
              subPath: public-key.pem
      restartPolicy: Always
      volumes:
        - configMap:
            items:
              - key: public-key.pem
                path: public-key.pem
            name: public-key
          name: public-key