{{- $secret_name := "opencrvs-superuser" }}
{{- $secret := lookup "v1" "Secret" .Release.Namespace $secret_name }}
{{- if not $secret }}
---
apiVersion: v1
kind: Secret
metadata:
  name: {{ $secret_name }}
  annotations:
    "helm.sh/resource-policy": keep
    "helm.sh/hook": pre-install,pre-upgrade
    "helm.sh/hook-weight": "0"
type: Opaque
data:
  SUPER_USER_PASSWORD: {{ default (randAlphaNum 32) .Values.credentials.super_user_password | b64enc }}
{{- end }}
