// App Connections

let internals = {
  connections: [
    {
      port: 3000,
      labels: ['web-app']
    },
    {
      port: 9000,
      labels: ['api']
    }
  ]
}

module.exports = internals.connections