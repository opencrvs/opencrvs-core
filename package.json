{
	"scripts": {
		"precommit": "lerna run --parallel precommit",
		"postinstall": "lerna bootstrap",
		"start": "lerna run --parallel start"
	},
	"devDependencies": {
		"husky": "^0.14.3",
		"lerna": "^2.11.0",
		"lint-staged": "^7.1.0",
		"prettier": "^1.12.1"
	}
}
