import * as React from 'react'
import styled, { StyledComponentClass } from 'styled-components'

export const EnglishText = styled.span`
  font-family: ${({ theme }: any) => theme.fonts.englishTextFont};
`
