A loader component with text:

```js
<Loader
  id="Loader"
  loadingText="Search result for KLNAQB"
  marginPercent={15}
  spinnerDiameter={70}
/>
```

A loader component without text:

```js
<Loader id="Loader" marginPercent={15} spinnerDiameter={70} />
```
