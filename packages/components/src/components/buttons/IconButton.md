### Icon button

```js
<IconButton
  id="myButton"
  onClick={() => alert('Hello')}
  icon={() => (
    <span style={{ color: '#fff', fontSize: '40px', lineHeight: '30px' }}>
      +
    </span>
  )}
/>
```
