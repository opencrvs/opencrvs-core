import * as pdfMake from 'pdfmake/build/pdfmake'
import { IDynamicValues } from '@opencrvs/components/lib/common-types'

export const printMoneyReceipt = (
  intl: InjectedIntl,
  registrant: Registrant,
  IssuerDetails: Issuer,
  amount: string,
  language: string,
  event: string
) => {
  return null
}
