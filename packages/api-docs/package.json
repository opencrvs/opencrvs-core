{
  "name": "@opencrvs/api-docs",
  "version": "2.1.0",
  "description": "OpenCRVS API documentation",
  "license": "MPL-2.0",
  "scripts": {
    "start": "pnpm build && serve -p ${API_DOCS_PORT:-3003} build",
    "build": "bash build.sh"
  },
  "private": true,
  "dependencies": {
    "@opencrvs/commons": "workspace:*",
    "@opencrvs/events": "workspace:*"
  },
  "devDependencies": {
    "serve": "^14.2.4"
  }
}
