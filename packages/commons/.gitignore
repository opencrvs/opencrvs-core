# See https://help.github.com/ignore-files/ for more about ignoring files.

# dependencies
/node_modules

# testing
/coverage

# production
/build

# typecheck-only output of `test:compilation` (tsc -b needs an outDir but
# nothing consumes it); kept out of /build so it cannot collide with the
# nx-cached outputs of the `build` target
/.tsc-tests

# misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*
