# ~/.tmuxinator/opencrvs.yml

name: opencrvs
root: ./

windows:
  - editor:
      layout: tiled
      panes:
        - apps:
            - yarn run start:bgd
        - deps:
            - yarn run compose:deps
